---
interface Props {
  class?: string;
  compressed?: boolean;
}

const { class: className = "", compressed = false } = Astro.props;
---

<div
  class:list={[
    "mx-auto w-full px-6 sm:px-4",
    compressed ? "max-w-[50rem]" : "max-w-4xl",
    className,
  ]}
>
  <slot />
</div>
